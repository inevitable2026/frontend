// ④ 감리단 보완 요구 공문 — T-04 의 입구.
//
// 지적 대상 19행이 표로 열거되어 있어야 한다. 「19행」이라는 숫자만 적힌 문서는
// 추출 결과가 숫자 하나로 끝나서 어느 행을 고쳐야 하는지가 카드에 실리지 않는다.

import { 문서, 머리표, 표, 서명란, 접수인, esc } from "../lib/render.mjs";
import { 현장, 회사, 인물, 감리원 } from "../data/site.mjs";
import { 등재행, 지적대상, 관리기간 } from "../data/risk-rows-august.mjs";

const 지적행 = 등재행
  .filter((r) => r.법적근거 === "" || r.개선후 === null)
  .map((r) => ({
    ...r,
    유형: r.법적근거 === "" ? "법적 근거란 공란" : "개선 후 위험도 미기재",
  }));

export const 지적정보 = {
  docId: "doc_7_v2n8qlxa",
  발신기관: 회사.감리,
  접수일시: "2026-08-19T10:05:00+09:00",
  replyDueBy: "2026-08-21T18:00:00+09:00",
  대상문서: "GGC-01-RA-2026-08-M",
  지적행수: 지적행.length,
  예상소요분: 180,
};

export default {
  id: "supervisor-correction",
  파일명: "05_감리단_보완요구_공문",
  kind: "기타",
  제목: "월간 위험성평가 회의록 보완 요구",
  먹이는조건: ["T-04"],
  html() {
    const 본문 = `
${접수인(`${회사.원청} 현장사무소`)}
${머리표([
  [["문서번호", "정우감리-2026-0819-11"], ["발신일자", "2026-08-19"]],
  [["수    신", `${회사.원청} 현장소장`], ["참    조", `${회사.원청} 안전관리자 ${인물.박정우.성명}`]],
  [["발    신", `${회사.감리} ${감리원.직위} ${감리원.성명}`], ["연 락 처", "shin@jungwoo-eng.co.kr"]],
  [["현    장", 현장.name], ["대상 문서", `${지적정보.대상문서} (관리기간 ${관리기간.시작} ~ ${관리기간.종료})`]],
  [["제    목", "월간 위험성평가 회의록 형식 요건 보완 요구의 건"]],
  [["회신 기한", "<b>2026-08-21(금) 18:00</b> 까지"]],
])}
<div class="clear"></div>

<h2>1. 검토 결과</h2>
<p>귀 현장이 제출한 위 대상 문서를 검토한 결과, 형식 요건을 갖추지 못한 행
<b>${지적행.length}건</b>이 확인되었습니다. 해당 사항이 보완되기 전에는 결재 상신 및
발주처 제출이 어려우니 회신 기한 내에 보완하여 회신하여 주시기 바랍니다.</p>

${표(
  [
    { 제목: "지적 유형", w: "34%", cls: "c" },
    { 제목: "건수", w: "12%", cls: "c" },
    { 제목: "해당 연번", w: "54%" },
  ],
  [
    ["법적 근거란 공란", `<b>${지적대상.법적근거공란.length}</b>행`, esc(지적대상.법적근거공란.join(", "))],
    ["개선 후 위험도 미기재", `<b>${지적대상.개선후미기재.length}</b>행`, esc(지적대상.개선후미기재.join(", "))],
    ["<b>합 계</b>", `<b>${지적행.length}</b>행`, "중복 없음"],
  ],
)}

<h2>2. 지적 대상 행 상세 (${지적행.length}건)</h2>
${표(
  [
    { 제목: "연번", w: "6%", cls: "c" },
    { 제목: "공종분류", w: "10%", cls: "c" },
    { 제목: "단위작업", w: "22%" },
    { 제목: "지적 유형", w: "20%" },
    { 제목: "보완 요구 내용", w: "30%" },
    { 제목: "보완 완료", w: "12%", cls: "c" },
  ],
  지적행.map((r) => [
    String(r.no),
    esc(r.공종분류),
    esc(r.단위작업),
    esc(r.유형),
    r.법적근거 === ""
      ? "해당 위험요인에 대응하는 조문 번호와 제목을 기재할 것"
      : "감소대책 적용 후의 빈도·강도·위험도를 산정하여 기재할 것",
    '<span class="blank sm"></span>',
  ]),
)}
<div class="legend">
보완 시 법적 근거는 조문 번호와 제목까지만 기재한다. 조문 본문을 옮겨 적을 필요는 없으며,
필요 시 공식 원문 조회 결과를 별첨한다.
</div>

<h2>3. 회신 요령</h2>
<ul class="tight">
  <li>보완된 회의록 사본 1부를 회신 공문에 첨부한다.</li>
  <li>보완이 불가능한 행이 있으면 그 사유를 행별로 기재한다.</li>
  <li>회신 기한: <b>2026-08-21(금) 18:00</b></li>
</ul>

<h2>4. 발신 명의</h2>
<p style="text-align:center;font-size:12pt;margin-top:8mm;letter-spacing:0.3em">${esc(회사.감리)}</p>
<div style="text-align:center;margin-top:3mm">
  <span style="display:inline-block;width:22mm;height:22mm;border:1px dashed #c00;border-radius:50%;color:#c88;font-size:7pt;line-height:22mm">(직인)</span>
</div>

${서명란(
  [
    { 소속: 회사.감리, 직위: "검토", 성명: "임세영" },
    { 소속: 회사.감리, 직위: "책임감리원", 성명: 감리원.성명 },
    { 소속: 회사.발주처, 직위: "사업관리", 성명: "노경민" },
  ],
  3,
  "발신 검토",
)}

<h2>수신처 회신란</h2>
${표(
  [
    { 제목: "구분", w: "22%", cls: "c" },
    { 제목: "담당", w: "16%", cls: "c" },
    { 제목: "처리일", w: "16%", cls: "c" },
    { 제목: "회신 내용", w: "46%" },
  ],
  [
    ["접수", '<span class="blank"></span>', '<span class="blank"></span>', '<span class="blank" style="min-width:58mm"></span>'],
    ["보완 작성", '<span class="blank"></span>', '<span class="blank"></span>', '<span class="blank" style="min-width:58mm"></span>'],
    ["회신 발송", '<span class="blank"></span>', '<span class="blank"></span>', '<span class="blank" style="min-width:58mm"></span>'],
  ],
)}
`;
    return 문서({
      제목: "보 완 요 구 공 문",
      문서번호: `정우감리-2026-0819-11 · ${지적정보.docId}`,
      머리말: `${esc(회사.감리)} &nbsp;|&nbsp; ${esc(현장.name)} 건설사업관리단`,
      본문,
    });
  },
};
