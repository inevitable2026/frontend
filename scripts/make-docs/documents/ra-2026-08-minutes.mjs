// ② 8월 월간 위험성평가 회의록 — T-01 · T-04 · T-05 · T-07 · S-02 의 대상.
//
// 21행이 등재되어 있고 세 종류의 빈칸이 서로 다른 감지를 먹인다.
//   법적 근거란 공란 12행 + 개선 후 위험도 미기재 7행 = 감리 지적 대상 19행 (T-04)
//   이행확인란 공란 9행 (S-02) — 그 중 하도급사 담당 4행
//   부록 A 의 추천값 이격 (T-07)
// 되메움작업·법면 행이 등재되어 있어야 강우 감지(T-01)가 대조할 상대를 갖는다.

import { 문서, 머리표, 표, 서명란, 결재란, esc, 칸, 체크, 강조행 } from "../lib/render.mjs";
import { 현장, 회사, 인물, 등급, 작성기준 } from "../data/site.mjs";
import {
  관리기간,
  등재행,
  추천값,
  추천값출처,
  지적대상,
  이행확인공란,
} from "../data/risk-rows-august.mjs";

const 담당명 = { 원청: 회사.원청, 골조: 회사.골조, 가설: 회사.가설, 양중: 회사.양중 };

function 점수(v) {
  return v ? `${v.빈도}×${v.강도}=<b>${v.빈도 * v.강도}</b>` : '<span class="blank"></span>';
}

const 참석자 = [
  { 소속: 회사.원청, 직위: "현장소장", 성명: 인물.황인철.성명 },
  { 소속: 회사.원청, 직위: "안전관리자", 성명: 인물.박정우.성명 },
  { 소속: 회사.원청, 직위: "안전보건담당", 성명: 인물.최민서.성명 },
  { 소속: 회사.원청, 직위: "공무팀장", 성명: 인물.정태식.성명 },
  { 소속: 회사.골조, 직위: "작업반장", 성명: 인물.이재훈.성명 },
  { 소속: 회사.골조, 직위: "공무담당", 성명: 인물.권상열.성명 },
  { 소속: 회사.가설, 직위: "반장", 성명: 인물.김성태.성명 },
  { 소속: 회사.양중, 직위: "신호수", 성명: 인물.오현수.성명 },
  { 소속: 회사.골조, 직위: "근로자대표", 성명: "문상혁" },
  { 소속: 회사.가설, 직위: "근로자대표", 성명: "천기범" },
  { 소속: 회사.원청, 직위: "보건담당", 성명: "유하늘" },
  { 소속: 회사.토공, 직위: "반장", 성명: 인물.배주형.성명 },
];

export default {
  id: "ra-2026-08-minutes",
  파일명: "02_8월_월간_위험성평가_회의록",
  kind: "위험성평가표",
  제목: "월간 위험성평가 회의록",
  먹이는조건: ["T-01", "T-04", "T-05", "T-07", "S-02"],
  html() {
    const 본문 = `
${결재란(["담당", "공무팀장", "안전관리자", "현장소장"])}
<div class="clear"></div>
${머리표([
  [["현 장 명", 현장.name], ["현장코드", 현장.code]],
  [["회의번호", "GGC-01-RA-2026-08-M"], ["관리기간", `${관리기간.시작} ~ ${관리기간.종료}`]],
  [["회의 일시", "2026-08-03(월) 15:00 ~ 16:40"], ["회의 장소", "현장 사무실 2층 회의실"]],
  [["회의 종류", "월간 위험성평가 회의(정기)"], ["참석 인원", `${참석자.length}명`]],
  [["평가 방법", 작성기준]],
  [["결재 예정일", "2026-09-02 (관리기간 종료 후 상신)"]],
])}

<h2>1. 회의 안건</h2>
<ul class="tight">
  <li>8월 관리기간 위험성평가 등재 행 ${등재행.length}건 검토 및 확정</li>
  <li>7월 등재 행의 감소대책 이행 결과 확인</li>
  <li>위험성평가 카탈로그 추천값과의 대조 결과 검토 (부록 A)</li>
  <li>우기 대비 토공 잔여 작업(되메움·법면) 관리 방안</li>
</ul>

<h2>2. 위험성평가 등재 행 (${등재행.length}건)</h2>
${표(
  [
    { 제목: "연번", w: "3.5%", cls: "c" },
    { 제목: "공종분류", w: "8%", cls: "c" },
    { 제목: "단위작업", w: "13%" },
    { 제목: "위험요인", w: "18%" },
    { 제목: "개선 전 위험도", w: "8%", cls: "c" },
    { 제목: "위험 감소대책", w: "18%" },
    { 제목: "개선 후 위험도", w: "8%", cls: "c" },
    { 제목: "담당", w: "7%", cls: "c" },
    { 제목: "기한", w: "6.5%", cls: "c" },
    { 제목: "법적 근거", w: "12%" },
    { 제목: "이행확인", w: "5%", cls: "c" },
  ],
  등재행.map((r) => {
    const cells = [
      String(r.no),
      esc(r.공종분류),
      esc(r.단위작업),
      esc(r.위험요인),
      `${점수(r.개선전)}<br><small>${등급(r.개선전.빈도 * r.개선전.강도)}</small>`,
      `<ul class="tight" style="font-size:7.6pt">${r.감소대책.map((m) => `<li>${esc(m)}</li>`).join("")}</ul>`,
      r.개선후
        ? `${점수(r.개선후)}<br><small>${등급(r.개선후.빈도 * r.개선후.강도)}</small>`
        : '<span class="blank"></span>',
      esc(담당명[r.담당] ?? r.담당),
      esc(r.기한),
      칸(r.법적근거),
      체크(r.이행확인),
    ];
    return r.no === 7 || r.no === 8 ? 강조행(cells) : cells;
  }),
)}
<div class="legend">
법적 근거는 조문 번호와 제목까지만 적는다. 조문 본문은 공식 원문 조회 결과로만 채운다.<br>
이행확인란의 <span class="chk">✔</span> 는 감소대책이 실제로 실행되었음을 확인했다는 표시다. 점선 칸은 미기재다.
</div>

<h2>3. 미기재 현황 (작성 시점 자체 점검)</h2>
${표(
  [
    { 제목: "항목", w: "26%" },
    { 제목: "건수", w: "8%", cls: "c" },
    { 제목: "해당 연번", w: "40%" },
    { 제목: "처리 예정", w: "26%" },
  ],
  [
    [
      "법적 근거란 공란",
      `<b>${지적대상.법적근거공란.length}</b>행`,
      esc(지적대상.법적근거공란.join(", ")),
      "조문 원문 조회 후 보완",
    ],
    [
      "개선 후 위험도 미기재",
      `<b>${지적대상.개선후미기재.length}</b>행`,
      esc(지적대상.개선후미기재.join(", ")),
      "대책 확정 후 산정",
    ],
    [
      "이행확인란 공란",
      `<b>${이행확인공란.length}</b>행`,
      esc(이행확인공란.join(", ")),
      "관리기간 중간 점검 시 확인",
    ],
    [
      "└ 그 중 하도급사 담당",
      `<b>${등재행.filter((r) => !r.이행확인 && r.하도급).length}</b>행`,
      esc(
        등재행
          .filter((r) => !r.이행확인 && r.하도급)
          .map((r) => `${r.no}(${담당명[r.담당]})`)
          .join(", "),
      ),
      "협력사 안전보건협의체 안건 상정",
    ],
  ],
)}

<h2>4. 회의 결정 사항</h2>
<ul class="tight">
  <li>연번 7(콘크리트 타설 및 다짐)의 개선 전 위험도는 현행 4×3=12 를 유지하되, 부록 A 의 추천값과의 이격이 커 별도 판정 절차를 둔다.</li>
  <li>연번 8(콘크리트 압송)의 통행 금지 구획은 압송 작업일마다 설치 상태를 확인한다.</li>
  <li>연번 1·2(되메움작업·법면 정리)는 강우 이후 착수 전 지반 상태를 확인한 뒤 진행한다.</li>
  <li>이행확인란 공란 ${이행확인공란.length}행은 관리기간 중간 점검일(8월 19일)에 일괄 판정한다.</li>
</ul>

${서명란(참석자, 4, "5. 참석자 서명")}

<div class="page-break"></div>
<h2>부록 A. 위험성평가 카탈로그 추천값 대조표</h2>
${머리표([
  [["점수 출처", 추천값출처.점수출처], ["카탈로그 상태", 추천값출처.originState]],
  [["관측 기간", 추천값출처.관측기간], ["갱신일", 추천값출처.갱신일]],
  [["검토 필요 플래그", `전 ${등재행.length}행에 설정됨 (reviewRequired = true)`]],
  [["자동 반영", "금지. 신뢰도가 낮은 폴백 경로 응답이므로 행별로 사람이 채택 또는 기각을 판정한다."]],
])}
${표(
  [
    { 제목: "연번", w: "4%", cls: "c" },
    { 제목: "단위작업", w: "20%" },
    { 제목: "현재 위험도", w: "9%", cls: "c" },
    { 제목: "추천 위험도", w: "9%", cls: "c" },
    { 제목: "이격", w: "6%", cls: "c" },
    { 제목: "표본수", w: "7%", cls: "c" },
    { 제목: "사고건수", w: "7%", cls: "c" },
    { 제목: "사망사고", w: "7%", cls: "c" },
    { 제목: "신뢰도", w: "7%", cls: "c" },
    { 제목: "판정", w: "24%" },
  ],
  등재행.map((r) => {
    const a = 추천값[r.no];
    const 현재 = r.개선전.빈도 * r.개선전.강도;
    const 추천 = a.빈도 * a.강도;
    const 이격 = Math.abs(추천 - 현재);
    const cells = [
      String(r.no),
      esc(r.단위작업),
      `${r.개선전.빈도}×${r.개선전.강도}=<b>${현재}</b>`,
      `${a.빈도}×${a.강도}=<b>${추천}</b>`,
      `<b>${이격}</b>`,
      String(a.표본),
      String(a.사고),
      String(a.사망),
      esc(a.신뢰도),
      '<span class="blank" style="min-width:34mm"></span>',
    ];
    return r.no === 7 ? 강조행(cells) : cells;
  }),
)}
<div class="legend">
판정란은 비워 둔다. 행별 채택 또는 기각과 그 사유는 판정 절차에서 기록한다.<br>
음영 행(연번 7)은 이격이 가장 크고 표본 ${추천값[7].표본}건 가운데 사망사고가 ${추천값[7].사망}건이다. 신뢰도는 ${추천값[7].신뢰도}이며 카탈로그 상태가 ${추천값출처.originState} 이므로 자동 반영하지 않는다.
</div>

<h2>부록 B. 이행확인 미표시 행 판정 대장 (중간 점검용)</h2>
${표(
  [
    { 제목: "연번", w: "5%", cls: "c" },
    { 제목: "단위작업", w: "20%" },
    { 제목: "담당", w: "14%", cls: "c" },
    { 제목: "구분", w: "10%", cls: "c" },
    { 제목: "실제 실행 여부", w: "17%" },
    { 제목: "확인자", w: "12%" },
    { 제목: "확인일", w: "10%" },
    { 제목: "비고", w: "12%" },
  ],
  등재행
    .filter((r) => !r.이행확인)
    .map((r) => [
      String(r.no),
      esc(r.단위작업),
      esc(담당명[r.담당] ?? r.담당),
      r.하도급 ? "하도급사" : "원청",
      '<span class="blank" style="min-width:24mm"></span>',
      '<span class="blank"></span>',
      '<span class="blank"></span>',
      r.하도급 ? "협의체 안건" : "",
    ]),
)}
<div class="legend">하도급사 담당 ${등재행.filter((r) => !r.이행확인 && r.하도급).length}행은 협력사 안전보건협의체에서 함께 확인한다.</div>
`;
    return 문서({
      제목: "월간 위험성평가 회의록",
      문서번호: "GGC-01-RA-2026-08-M",
      머리말: `${esc(회사.원청)} &nbsp;|&nbsp; ${esc(현장.name)} &nbsp;|&nbsp; 관리기간 ${esc(관리기간.시작)} ~ ${esc(관리기간.종료)}`,
      본문,
    });
  },
};
